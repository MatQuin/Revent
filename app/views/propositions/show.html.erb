<div class="container margin-top-negative">
  <%= render 'shared/card', element: @event %>
  <div class="row">
    <div class="col-6">
      <div class="proposition-show-card">
        <%# <%= cl_image_tag @event.photo.key, height: 300, width: 400, crop: :fill %>
        <h1><%= @proposition.description %></h1>
        <p class="event-show-title"><%= @event.address %></p>
    </div>
    <div class="proposition-show-card">
      <h3>Activity details</h3>
      <p> From <%= @event.start %> to <%= @event.end %> </p>
      <p><%= link_to "Add Propositions", "/propositions/new"%></p>
      </div>
    </div>
    <div class="col-6">
      <div class="event-show-card">
        <%# <%= render "activities/new" %>
        <h3>Activity Propositions</h3>
        <p><%= link_to "Add propositions", new_activity_proposition_path(@activity) %></p>
      </div>
    </div>
</div>
