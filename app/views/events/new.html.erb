<div class="container">
  <div class="row mt-5" data-controller="event">
    <div class="col-6">
      <%= render "form", event: @event, user: @user %>
    </div>

    <div class="col-6 col-6 bg-white rounded-4 p-3">
      <div class="card">
        <%= render "preview", event: @event %>
      </div>
    </div>
  </div>
</div>
